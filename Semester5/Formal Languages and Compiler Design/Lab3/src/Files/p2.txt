start{
    a typeof int;
    b typeof int;
    bagă(a);
    bagă(b);
    cattimp (a <> b) fă{
        oare(a > b) atunci{
            a = a - b;
        }
        altfel{
            b = b - a;
        }
    }

    arată(a);
}